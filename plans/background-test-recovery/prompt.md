# Execution Prompt

Read the adjacent plan, contract, todo, context and repository `CLAUDE.md`. Implement exact RED first. Preserve public `run_tests`/`get_test_job` compatibility, but make `get_test_job` observational and add explicit `nudge_test_job`. Do not operate a live Unity Editor. Run the eight C# exact methods only in a temporary copy of `TestProjects/UnityMCPTests` through an independent headless Unity `2022.3.62f3c1`, with `assemblyNames=MCPForUnityTests.EditMode` and the semicolon filter recorded in `plan.md`; never route them through MCP `run_tests` because six reset the outer job state. Do not hand-edit `MCPForUnity/package.json`: after every test passes and revision 4 is freshly authorized, the existing `make release` flow alone generates and commits it. Do not modify the release publisher, `Makefile` or existing release tests.
